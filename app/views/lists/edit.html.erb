<div class="nav nav-tabs">
<%= nav_link '1. Choose Recipes', new_menu_path %> 
<%= nav_link '2. Choose Items', add_items_path(@list) %> 
<%= nav_link '3. Review Shopping List', edit_list_path(@list) %>
</div>

<p>
  <strong>List:</strong>
  <%= @list.id %>
</p>

<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>

<p>
  <strong>Comment:</strong>
  <%= @list.comment %>
</p>

<p>
  <strong>Items:</strong>
  <% @list.items.each do |item| %>
    <%= item.name %>
  </br>
  <% end %>
</p> 

